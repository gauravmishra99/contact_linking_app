export interface Contact {
    "id" ?: number,
    "phonenumber": string,
    "email": string,
    "linkedid" ? : number,
    "linkprecedence": string,
    "createdat": string,
    "updatedat": string,
    "deletedat" ?: string
}